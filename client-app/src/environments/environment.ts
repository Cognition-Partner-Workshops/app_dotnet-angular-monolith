/**
 * Development environment configuration.
 * Used when running `ng serve` locally. The empty `apiUrl` means API calls
 * use relative paths, which are proxied to the .NET backend during development.
 */
export const environment = { production: false, apiUrl: '' };
