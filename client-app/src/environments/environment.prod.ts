/**
 * Production environment configuration.
 * Used when building with `ng build --configuration production`. The empty `apiUrl`
 * means API calls use relative paths, served by the same host as the static frontend assets.
 */
export const environment = { production: true, apiUrl: '' };
